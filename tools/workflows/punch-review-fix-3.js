export const meta = {
  name: 'punch-review-fix-3',
  description: 'Review and fix in place, one owner per area, two at a time, every fix logged to disk as it lands',
  phases: [
    { title: 'Wave 1', detail: 'machine flow screens, phone and the live machine' },
    { title: 'Wave 2', detail: 'case study and diagrams, then copy and access across everything' },
  ],
}

const P = 'C:/Claude Database/punch-exercise'
const HOUSE = `
PROJECT: a design showcase for a PunchApp design task done for Robotenc, in ${P}/showcase (vanilla HTML, CSS and JS), served at http://localhost:5770 by \`node tools/serve-showcase.mjs\` (if it does not answer, start it in the background from ${P}). Includes <!-- include:path --> expand server-side; /sections.css and /mscreens.css concatenate showcase/sections/*.css and showcase/mscreens/*.css.
WHAT EXISTS: top bar with three modes (Machine screen, Mobile app, Design system), "The brief" drawer, "Read case study", "Customise". Global THEME html[data-variant="arena|reference"], APPEARANCE html[data-appearance="dark|light"], LOGO (PunchApp mark or six others, default Fist; <img data-logo-img> takes the chosen mark). MACHINE: a screen bar over the glass switches seven 1080 x 3840 screens: attract (leaderboard between players), scan (QR to download the app plus How to use card; states waiting, linked, holding, missed), countdown (Punch now, 10 s; a demo punch lands and opens record), result (#screen, nine configurable sections), stats (ranks and stats), score (big score), record (New record). Flow screens are parts/mscreen-KEY.html + mscreens/KEY.css + mscreens/KEY.js and should share identical logo lockup, sponsor strip, light shapes and photo card. MOBILE: phone frame (iPhone 17 Pro Max or Android), screens scan, connect, topup, checkout, paid, punch (Make your punch, countdown synced to the machine), hit, ranks, feed, profile; five tab bar styles whose middle button is the chosen logo; beside the phone on wide windows a live machine (this page as ./?embed=machine in iframe #linkedFrame) that follows the phone over a BroadcastChannel. CASE STUDY: an Apple product page style long read inside #caseScroll with two live machine embeds (./?embed=machine&follow=0) and three live diagrams (parts/case-state.html, case-run.html, case-geometry.html with case-diagrams.css/js, cd- prefixed classes; the countdown screen also uses cd- classes and one collision, .cd-h, already had to be scoped).
HOOKS: window.showcase.mode(m), .mscreen(key, opts) (countdown {punch:false} runs out; scan {linked, holding, missed}), .theme('arena'|'reference'), .appearance('dark'|'light'), .logo(key), .zoom(n); window.punchApp.go(page, opts), .device('iphone'|'android'), .style('board'|'connect'|'nav', i), .credits (get/set), .score.
TOOLS: node tools/shoot.mjs <url> <outDir> <width> <height> <plan.json> (steps {wait}, {eval}, {click: selector} a REAL mouse click, {key}, {shot: name, clip?, full?, scale?}; prints console errors; REDUCED_MOTION=1 in the environment emulates reduced motion). Write plans with the Write tool. The loader takes about 3.5 s: start plans with {eval: "localStorage.clear(); location.reload(); 1"}, {wait: 4200}. Inside one plan, eval steps share one page scope, so never redeclare a const across steps (wrap in a block). Open PNGs with the Read tool and LOOK.
HOUSE RULES: no em dashes or en dashes in visible copy; numerals only where they are content; a hairline or bar between two words reads as a dash (the red rules flanking the phone's "Scan machine QR code" line and the Brackets connect line are from the user's own Figma frames: keep them); no emoji; no gradient text; visible focus; reduced motion respected; tokens for colour and type so every look works; nothing clipped, overlapping or colliding.

HOW TO WORK (important, a usage limit has killed two earlier review runs and lost all their findings):
- You REVIEW AND FIX your area in one pass. Find a real problem, fix it properly, verify the fix with a fresh render, then log it, then move on. Worst problems first.
- IMMEDIATELY after each fix, append a short block to ${P}/build/review/LOG_FILE (create it if missing) with: what was wrong, where, what you changed (file and selector or function), how you verified. Do this with a Bash append (cat >> file <<'EOF' ... EOF) or by rewriting the file with the Write tool. Never batch the log to the end.
- Work efficiently: about 70 tool calls in total. Prefer a few well-aimed renders (contact sheets of several shots with Python PIL) over many single ones. Stop when the important problems in your area are fixed.
- Edit ONLY the files you own (listed below). If something outside them needs a change, write it in the log under "For others" and in your reply.
`

const W1 = [
  { log: 'log-machine.md', label: 'machine flow screens', prompt: `YOUR AREA: the six machine flow screens. FILES YOU OWN: showcase/parts/mscreen-*.html, showcase/mscreens/*.css, showcase/mscreens/*.js, and the machine flow frame rules at the end of showcase/styles.css (.mpagebar, .mscreen, embed rules). CHECK: each screen in Arena dark, Arena light, Reference dark, Reference light at zoom 30 (window.showcase.zoom(30); clip #machine using its getBoundingClientRect; hide .topbar with visibility hidden while shooting); the countdown while counting, in the last three seconds, at the hit and at time up ({punch:false}); scan in its four states; record with its confetti; stats and score. Compare with the Figma frames if anything looks missing (file aDk4RGbovg0KKhtUBokHjk, nodes 187:2651 leaderboard, 187:3155 scan, 187:3094 countdown, 187:2852 stats, 187:2940 big score, 187:3006 record; get_screenshot). Known and worth checking first: the shared parts differ between screens (the AD tag is 20 px on one screen and 16 px on another; logo lockup top offset differs by frame): make the logo lockup, sponsor strip with its AD tag (about 22 px so it reads from a metre), light shapes and photo card identical on all six, ideally by moving the shared rules into one place (for example the end of styles.css under .mscreen) and deleting the per-screen copies. Also check for selector collisions with other stylesheets by reading computed styles of each screen's headline, number and labels (case-diagrams.css uses .cd-* too). Check the embed at 300 x 1066 (http://localhost:5770/?embed=machine then window.showcase.mscreen(key)).` },
  { log: 'log-phone.md', label: 'phone and the live machine', prompt: `YOUR AREA: the phone and the live machine beside it. FILES YOU OWN: showcase/parts/phone.html, showcase/parts/phone-pay.html, showcase/mobile.css, showcase/mobile.js, showcase/nav.css, showcase/pay.css, showcase/pay.js, and in showcase/app.js ONLY the blocks "the machine flow" (setMscreen and the embed listener) and "the machine beside the phone" (syncLinked). CHECK at 1600 x 1000 in mobile mode (window.showcase.mode('mobile')): with window.punchApp.credits = 3, run scan (click #mScanBtn), connect, the punch step and the landing with REAL clicks, sampling the phone's countdown number and the live machine's number (document.getElementById('linkedFrame').contentDocument, its .mscreen-countdown number element) every 200 ms: they must match; then the machine must show record then result while the phone shows Your hit; then Punch again; then Cancel on a punch step (credit returns, machine back to scan); then credits 0 through top up and pay back to punch. Then theme, appearance and logo changes in Customise must reach the live machine within a second. Check the live machine panel layout at 1280, 1440, 1920 wide and with Customise open (no overlap with the phone or the panel, hidden when it cannot fit), the punch step in all four looks on iPhone and Android including time up (window.punchApp.go('punch', { miss: true })), the How to use sheet with the new wide illustration, and the tab bar's logo button in all five styles with the PunchApp and Fist logos. Stuck timers after leaving a page, drift, wrong machine screen and console errors are the priorities.` },
]

const W2 = [
  { log: 'log-case.md', label: 'case study and diagrams', prompt: `YOUR AREA: the rebuilt case study and its three diagrams. FILES YOU OWN: showcase/parts/case.html, showcase/case.css, showcase/case.js, showcase/parts/case-state.html, showcase/parts/case-run.html, showcase/parts/case-geometry.html, showcase/case-diagrams.css, showcase/case-diagrams.js. Open it with a real click on #openCase and scroll #caseScroll through the whole page in steps at 1440 x 900 and 390 x 844 (set scrollTop in eval steps, remember not to redeclare consts across steps), mid-animation and in pinned sequences, and once with REDUCED_MOTION=1. Known issue to check first: in the phone chapter, "The machine follows the phone" shows a small machine beside the phone that renders as a plain red bar instead of a screen; make it a real miniature of the machine glass (a live embed is too heavy for a third copy; a still of the countdown or result drawn small, or a scaled image, is fine) or remove it and let the copy carry the idea. Then: no text stuck faded or hidden at any scroll position, nothing overlapping during pinned sequences, the two live embeds show their intended screens and never follow the phone, the diagrams legible and animated at every width, the chapter bar, close button, data-go-mode buttons, Escape and focus return, no sideways scroll at 390, no console errors. Scope every case-diagrams.css rule that is not already under .cd-state, .cd-run or .cd-geometry (the roots) so none can leak into the machine's countdown screen, which also uses cd- class names; verify by checking the countdown headline computed font size is still 150 px in Arena (window.showcase.mode('machine'); window.showcase.mscreen('countdown', {punch:false})).` },
  { log: 'log-copy.md', label: 'copy and access across everything', prompt: `YOUR AREA: copy, accessibility and the house rules across EVERYTHING that changed recently: the seven machine screens, the punch step, the How to use sheet, the tab bar logo button, the live machine panel, the case study and its diagrams. You run after the other owners, so you may edit any showcase file, with small targeted edits only. Extract visible text of each (machine screens via window.showcase.mscreen, phone pages via window.punchApp.go, the case study via #openCase) and fix em dashes (U+2014), en dashes (U+2013), decorative numerals, emoji, typos, and inconsistent names (make the machine and phone agree on the strike moment wording and on "PunchApp" as the brand word in visible copy except the wordmark image); measure contrast of text against the real pixels behind it in all four looks on the new screens and fix what fails 4.5:1 (3:1 for large text); check accessible names, image alt text, heading order in the case study, focus visibility, that the countdown live regions are not chatty (announce the start, the last three seconds at most, and the landing), and reduced motion.` },
]

const run = (t, phaseName) => agent(HOUSE.replace('LOG_FILE', t.log) + '\n' + t.prompt + '\nWHEN DONE reply with the list of what you fixed (it should match your log), what you checked and found fine, and anything for others.', { label: t.label, phase: phaseName })

phase('Wave 1')
const w1 = await parallel(W1.map((t) => () => run(t, 'Wave 1')))
phase('Wave 2')
const w2a = await run(W2[0], 'Wave 2')
const w2b = await run(W2[1], 'Wave 2')
return { wave1: w1, case: w2a, copy: w2b }
