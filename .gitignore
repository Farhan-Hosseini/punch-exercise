# Intermediate renders, captures and verification output. Everything here is rebuilt by the tools in tools/ and
# runs to several gigabytes, so it stays out of the repository.
build/

# Dependencies (the fonts and the QR library; npm install restores them)
node_modules/

# Local scratch
*.log
.DS_Store
Thumbs.db
