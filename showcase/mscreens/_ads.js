// The sponsor slot (parts/ms-ad.html) needs no script: its 7 s loop is CSS keyframes in hard steps (mscreens/_ads.css),
// and it restarts on its own each time a flow screen is shown, the way a banner GIF starts over.
(() => {
  'use strict'
})()
